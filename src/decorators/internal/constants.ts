/**
 * `@nestjs/swagger`'s method-level parameters metadata key. Mirrored verbatim
 * from `@nestjs/swagger/dist/constants.js`'s `DECORATORS.API_PARAMETERS` —
 * inlined as a string literal so we don't reach into the package's deep
 * import paths (it isn't part of the package's public `exports` map).
 */
export const API_PARAMETERS_KEY = 'swagger/apiParameters';
